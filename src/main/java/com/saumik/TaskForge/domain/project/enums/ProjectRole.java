package com.saumik.TaskForge.domain.project.enums;

public enum ProjectRole {
    MANAGER,  // full control over project, members, tasks, visibility
    MEMBER    // can create/update tasks assigned to them, read access
}