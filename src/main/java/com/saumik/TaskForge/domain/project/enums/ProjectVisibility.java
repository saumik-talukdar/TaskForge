package com.saumik.TaskForge.domain.project.enums;

public enum ProjectVisibility {
    PUBLIC_TO_ORG,  // visible to all org members
    PRIVATE         // visible only to project members + org admins
}